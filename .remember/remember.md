# Handoff

## State
Two repos, both pushed & clean: Heritage-Sciences (paper) at `903225b`, mogao-digital-twin at `30e9cf2`. Paper reframed to npj house style (prose contributions, superscript-numbered natbib citations, unsrtnat — sn-nature.bst needs sn-jnl class so not used). Compiles clean (48pp). Codebase audit done: runtime Eₐ aligned to measured 53/49/43, dead code removed, docs reconciled.

## Next
1. Contribution REORDER (paused, not started — no edits made). Agreed order: framework → 3D rendering → multi-mechanism risk state (weave in live-data/prediction/maintenance) → replica calibration. UNDECIDED: contribution #1 as heritage-framed ("predictive DT for heritage objects, engineered via MDE", my rec) vs plain software/engineering-first. One pass over introduction.tex + Discussion.tex (+ abstract check).

## Context
- User continuing on Mac — `git pull` both repos first; papers/*.pdf + sn-nature.bst are gitignored (local only), re-copy if needed.
- 4 contributions currently woven as prose in introduction.tex para 5 + Discussion "First/Second/Third/Fourth". Keep 4 (don't add a 5th).
- npj Heritage Science: no strict word limit; target ~6.5–7.5k main text. Paper ~7.7k, fine.
